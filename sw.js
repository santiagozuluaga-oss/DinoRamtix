self.addEventListener('push',event=>{let data={title:'DinoRamtix',body:'Tienes un nuevo mensaje',url:'/'};try{if(event.data)data={...data,...event.data.json()}}catch(_){}event.waitUntil(self.registration.showNotification(data.title,{body:data.body,icon:data.icon||'/assets/dino-profile.jpg',badge:data.badge||'/assets/dino-profile.jpg',data:{url:data.url||'/'},vibrate:[100,50,100]}))});
self.addEventListener('notificationclick',event=>{event.notification.close();const url=event.notification.data?.url||'/';event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(cs=>{for(const c of cs){if('focus' in c){c.navigate(url);return c.focus()}}return clients.openWindow(url)}))});